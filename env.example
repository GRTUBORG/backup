REMOTE_USER=root
REMOTE_HOST=CHANGE_ME
WATCH_DIR=/root/node
LOCAL_BACKUP_DIR=/root/node_backups
REMOTE_DIR=/root/backup_node/node-1
REMOTE_PORT=22
# SSH_KEY_PATH=/root/.ssh/id_ed25519

# --- transfer-backup.sh (run on reserve server) ---
# SOURCE_USER=root
# SOURCE_HOST=CHANGE_ME
# SOURCE_ARCHIVE_PATH=/root/node_backups/node_backup_YYYY-MM-DD_HH-MM-SS.zip
# SOURCE_PORT=22
# SOURCE_SSH_KEY_PATH=/root/.ssh/id_ed25519
# DEST_DIR=/root/node
# EXTRACT_SUBDIR=
# KEEP_LOCAL_ARCHIVE=true
# LOCAL_ARCHIVE_DIR=/tmp/backup-transfer
